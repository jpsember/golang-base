class {
    // Unique id
    int id;

    // Unique name
    string name;
    enum UserState state;

    // Unique email
    string email;
    string password;
    *byte password_hash;
    int password_salt;
    enum UserClass user_class;

    string reset_password_secret;
}

sql { index name index email }
